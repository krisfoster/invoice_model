{
  "name": "M3 Max 64GB Optimized Profile",
  "description": "High-performance training configuration for M3 Max with 64GB RAM",
  "hardware": {
    "chip": "Apple M3 Max",
    "memory": "64GB",
    "gpu_cores": "30-40",
    "device": "mps"
  },
  "model": {
    "model_name": "distilbert-base-uncased",
    "max_length": 512,
    "learning_rate": 3e-5,
    "batch_size": 32,
    "num_epochs": 10,
    "warmup_steps": 500,
    "weight_decay": 0.01,
    "gradient_accumulation_steps": 1
  },
  "training": {
    "save_steps": 500,
    "eval_steps": 500,
    "logging_steps": 50,
    "save_total_limit": 5,
    "load_best_model_at_end": true,
    "metric_for_best_model": "f1"
  },
  "performance_notes": {
    "expected_speed": "1-3 minutes per epoch",
    "memory_usage": "~20-30% of available RAM",
    "gpu_utilization": "70-90%",
    "can_try_larger": "batch_size up to 64 if needed"
  },
  "alternative_configs": {
    "ultra_fast": {
      "description": "Maximum speed, potentially less stable",
      "batch_size": 48,
      "learning_rate": 4e-5,
      "gradient_accumulation_steps": 1
    },
    "memory_conservative": {
      "description": "If running other apps simultaneously",
      "batch_size": 24,
      "learning_rate": 3e-5,
      "gradient_accumulation_steps": 1
    },
    "large_model": {
      "description": "For BERT-base or larger models",
      "model_name": "bert-base-uncased",
      "batch_size": 16,
      "learning_rate": 2e-5,
      "gradient_accumulation_steps": 2
    }
  },
  "tips": [
    "Close unnecessary apps before training for maximum performance",
    "Monitor with: sudo powermetrics --samplers gpu_power -i 1000",
    "Expected GPU usage: 70-90% during training",
    "Can increase batch_size to 48 or 64 for even faster training",
    "Use --max-length 256 if invoices are short to speed up further"
  ]
}
